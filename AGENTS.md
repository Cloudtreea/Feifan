# 历史对对碰：项目协作说明

## 项目定位

- 项目是浏览器端单机历史记忆翻牌游戏，名称为“历史对对碰”。
- 唯一应用文件是 `memory_station.html`；HTML、CSS、JavaScript 均内嵌其中。
- 保持零依赖、无构建步骤、无服务器，直接用现代浏览器打开即可运行。
- 界面与文案统一使用简体中文。

## 目标设备与交付方式

- 目标学练机分辨率为 1920×1200，使用横屏触控。
- 主交付物是单个 `memory_station.html`，公司工具上传后生成 OSS 访问地址。
- 本地保留同一个 HTML 文件作为断网备用，不维护第二套代码。
- 首发验收以 1920×1200 横屏为基准，按钮和卡片目标尺寸不小于约 44×44 CSS 像素。
- 正式上传后记录文件版本、上传时间和 OSS 地址，并检查缓存是否仍指向旧版本。

## 产品目标与功能

- 用短回合、低门槛玩法改善学生“执行启动感”低的问题。
- 用时间线索、事件影响、即时反馈和成长记录改善学生“方法掌控感”弱的问题。
- 提供基础、进阶、挑战三档难度和 3×3、4×4、5×5 三种棋盘。
- 支持翻牌、匹配、失败盖回、计时、翻牌计数、得分、每日挑战、成长报告和浏览器音效。
- 开局直接显示配对线索：3×3 为 1 条、4×4 为 2 条、5×5 为 3 条。
- 线索只直接呈现“卡片 A ↔ 卡片 B”，不添加解释性描述。
- 线索区保留一个提示按钮；每次只短暂翻开一组中的一张卡片，盖回后该位置持续高亮；提示不增加翻牌数、不自动完成配对，也不扣分。
- 奇数格棋盘加入一张幸运星，翻开后直接完成一组。

## 历史题库约束

- 题库位于 `BANK`，每组格式为 `[左卡文本, 右卡文本, 历史类别, 左标签, 右标签]`。
- 内容以高中教材的高频主干知识为主，中国史与世界史混合，避免冷僻史实和冗长描述。
- 适合准确纪年的内容采用“年份—历史事件”；适合阶段概括的内容采用“时间段—历史事件”。
- 可采用“关键历史事件（或制度）—核心影响”，影响必须使用简洁、准确且有唯一指向的教材式表述。
- 历史类别固定为：中国古代史、中国近现代史、世界近代史、世界现代史。
- 每档当前包含 16 组，每类 4 组；修改后每档至少保留 12 组以支持 5×5 棋盘。
- 同一难度内左右卡文本必须各自唯一，不能形成交叉或歧义配对。
- 精确年份不省略“年”；时间段统一使用“时期”“世纪”或起止年份，选择符合教材叙述习惯的粒度。
- 事件和影响保持简短、准确、适合卡片阅读；存在史学口径争议的内容不得加入首版题库。
- 修改题库时人工核对史实、教材表述、唯一性和四类内容数量，不能只依赖字符串检查。

## 抽题与游戏状态

- `selectPairs()` 负责打乱题库、均衡四类历史内容并排除文本重复，不要绕过它直接抽题。
- 每局普通卡片按共同 `id` 判断匹配，不直接比较显示文本。
- 任一时刻最多只能有两张尚未完成匹配的普通卡片正面朝上。
- 线索必须来自本局已抽取的普通配对；每日挑战中线索随固定题组保持一致。
- 线索始终保持可读，对应配对完成后标记为“已掌握”。
- 匹配成功的卡片保持翻开并显示绿色；失败卡片按设置延迟自动盖回。
- 重新开始必须重置计时器、翻牌数、配对数、首张卡引用和交互锁。
- 每日挑战使用日期、难度和棋盘规模生成固定随机种子；同一天同配置应得到相同题组。

## 卡片与响应式布局

- 棋盘使用与页面不同的深紫色背景，保持卡片边界清晰。
- `render()` 将棋盘规模写入 `board.dataset.size`，CSS 按 3×3、4×4、5×5 设置字号。
- 卡片内容必须通过 `textContent` 写入 `.card-content`，不得拼接不受控 HTML。
- 历史事件较长时允许在中文语义边界换行，并保持 `text-wrap: balance`。
- 修改卡片样式后同时检查三种棋盘、1920×1200 横屏和 720px 以下回退布局。

## 计分与本地数据

- 最终得分范围 0–1000，精准度占 70%，速度占 30%。
- 速度目标：3×3 为 35 秒、4×4 为 75 秒、5×5 为 130 秒。
- 历史版 localStorage 键固定为 `historyMatchUserV1`；旧数学键 `memoryStationUserV2` 只用于首次迁移设置，不得覆盖或删除。
- 首次没有历史数据时，只迁移旧数据的 `settings.sound` 和 `settings.delay`，不迁移数学成绩与打卡。
- 数据包含 `dataVersion`、`history`、`settings`、`daily`；历史记录必须带 `subject: "history"`。
- 历史最多保留 50 条；清除记录时保留设置，只清空历史成绩和历史打卡。
- 读取数据时容忍 localStorage 缺失或 JSON 损坏，不应阻断游戏启动。
- 黑客松版本保持纯本地存储，不调用后台接口，不在 HTML 中保存令牌或密钥。
- 后续同步约定记录在 `SYNC_SPEC.md`，实现同步前必须先阅读该文件。

## 修改后的验证清单

- 验证首页难度、棋盘选择、开始游戏、返回主页和重新开始。
- 验证匹配成功、匹配失败、交互锁、幸运星和整局完成判定。
- 验证三种棋盘卡片数量、配对分母及各类卡片标签。
- 检查三档各 16 组，确保左右文本唯一、配对无歧义且教材表述准确。
- 多次抽题，确认中国史与世界史类别分布均衡。
- 验证刷新后历史设置、成绩、今日挑战和连续打卡可以恢复。
- 验证旧数学成绩不进入历史成长报告，但旧音效和停留时间设置可以迁移。
- 验证成长报告分别统计基础、进阶、挑战三档历史数据。
- 保持单文件离线运行，不无故引入框架、外部资源或网络请求。
