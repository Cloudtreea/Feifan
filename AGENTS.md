# 历史对对碰：项目协作说明

## 上下文与交接纪律

- 不依赖聊天上下文长期保存项目事实。出现新的关键产品决策、数据口径、接口约束、验收结论、已知风险或未完成阻塞时，应及时更新本文件或对应项目文档。
- 在长任务推进、阶段交接或可能发生上下文压缩前，检查本文件是否已覆盖最新的重要信息；只记录仍然有效的结论，不写入探索过程、重复讨论和已否定方案。
- `AGENTS.md` 保存长期有效的项目约束；具体执行进度写入 `TODO.md`，运行与验收步骤写入 `README.md`，避免把短期操作细节全部堆入本文件。

## 项目定位

- 项目是面向学练机的浏览器端历史记忆翻牌游戏，名称为“历史对对碰”。
- 学生端为 `memory_station.html`；教师端演示后台为独立的 `teacher_dashboard.html`，两者均为无依赖单文件。
- 保持零依赖、无构建步骤、可离线运行，界面统一使用简体中文。
- 目标设备为 1920×1200 横屏触控学练机，主要触控目标不小于约 44×44 CSS 像素。
- 主交付物通过公司工具上传并生成 OSS 地址，本地同一文件作为断网备用。

## 产品目标与结构

- 用短回合、低门槛玩法改善学生“执行启动感”低的问题。
- 用明确关系、即时反馈、学习证据和成长记录改善学生“方法掌控感”弱的问题。
- 首页三个入口固定为：中国古代史、中国近现代史、世界史。
- 3×3、4×4、5×5 表示游戏强度和记忆负荷，不代表知识难度。
- 同一知识点允许存在多种配对关系，但同一局只能抽取一种 `relationType`。
- 当前关系包括：`time-event`、`event-impact`、`system-impact`。

## 题库结构与约束

- `BANK` 按 `moduleId` 分组，每道题由 `pair()` 生成对象。
- 可管理题库使用独立 localStorage 键 `historyQuestionBankV1`；学生端首次启动时以 `BANK` 初始化，教师端修改后学生端刷新生效。
- 教师端支持按板块和关系新增、编辑、删除以及JSON导入导出；删除和导入不得使任一板块、任一关系少于12组。
- 教师端题库管理界面隐藏内部题目 ID 和英文编码；专题及卡片标签使用中文受控选项。“易混知识”不作为教师录题字段，已有底层数据仅为兼容保留。`low` 状态在界面称为“停用”，表示保留题目但不参与随机抽取。
- 每题必须包含稳定的 `pairId`、`knowledgeId`、`moduleId`、`unitId`、`topicId`、`relationType`、`importance`、`cognitiveLevel`、`cardA` 和 `cardB`；易混知识使用 `confusableGroup`。
- 同一知识点跨关系复用时使用相同 `knowledgeId`，具体配对使用不同 `pairId`。
- 每个板块的每种关系至少保留 12 组，确保 5×5 可抽取 12 个普通配对。
- `question_bank_manifest.json` 是后端覆盖率分母的题库清单。修改内置题库并改变唯一 `knowledgeId` 数量时必须同步更新清单；测试必须校验清单与 `memory_station.html` 的题库一致。
- 内容必须以高中历史课程标准、高考评价体系和公开高考试题评析为筛选依据，采用高频主干知识，避免把冷僻朝代建立年份当作核心记忆目标，也不得用未经核验的“命中率”替代内容审核。
- 精确年份不省略“年”；更适合阶段概括的内容使用“时期”“世纪”或起止年份。
- `selectPairs()` 必须先锁定一种关系，再排除显示文本冲突并抽题；不得绕过它混抽关系。
- 同局角标按关系统一显示，底层题目标签只用于内容与分析，不得造成多规则错觉。

## 游戏与线索规则

- 支持翻牌、匹配、失败盖回、计时、翻牌数、得分、每日挑战和幸运星。
- 普通配对通过共同局内 `id` 匹配，不直接比较显示文本。
- 任一时刻最多有两张尚未匹配的普通卡片正面朝上。
- 3×3、4×4、5×5 分别直接显示 1、2、3 组“A ↔ B”线索。
- 提示按钮每次只短暂翻开一组中的一张卡片，不计入翻牌数；盖回后位置持续高亮并标记“提示1/2/3”。
- 重新开始和返回主页必须记录未完成对局，再重置计时器、翻牌状态、交互锁和本局数据。
- 每日挑战固定使用 4×4 棋盘，并使用日期和板块生成固定随机种子；同一天同板块必须得到相同题组和关系。
- 首页提供独立“游戏规则”入口，规则弹窗必须说明：单局关系、首次未知翻牌豁免、线索与提示、幸运星单独计为一组、90%/10%评分权重、各棋盘参考时限和连续翻对规则。
- “游戏规则”始终排列在“设置”左侧；进入游戏后两者一起移动到右侧控制区，返回首页后必须一起回到页头操作区且保持顺序。
- 首页成长区域只展示成就墙入口，完整成就在独立弹窗中展示；成就由稳定 `id`、名称、图标、解锁说明和基于本地学习数据的判定函数组成，新增成就时不得改动既有成就 ID。
- 成就覆盖完成局数、单局分数、连续正确配对、不同棋盘、每日挑战、板块探索和无提示通关；完成对局时必须在结算弹窗展示本局新解锁成就。
- 每个主要成就方向必须设置由低到高的多阶段梯度，并保留长期上限；成就墙按类别分组展示，避免只有一次性目标或过早失去挑战性。
- 隐藏成就通过 `hidden` 与 `revealed` 控制，在前置成就达成前不显示名称、条件或锁定占位。例如1000局成就只在100局成就达成后出现。
- 成就墙内提供只读测试预览，可切换隐藏成就临界条件；测试数据只存在于当前页面内存，不得写入 `localStorage`、学习事件、同步队列或真实成绩。
- 完成棋盘后进入一次关底检测：从本局普通配对中抽取一组，以正向或反向四选一方式确认。检测计时从棋盘完成后单独记录，不改变游戏积分；结果作为独立学习事件和更强的知识掌握证据保存。
- 结算弹窗提供“回顾棋盘”入口；进入后保留所有已完成卡片的正面内容，并在右侧提供“返回结算”，不得重新计时、重置棋盘或生成新对局。
- 面向学生的设置页默认隐藏演示身份、同步诊断、手动同步、清除记录和成就测试入口；仅在地址加入 `?debug=1` 时显示这些开发验收工具，后台自动保存与同步不受影响。

## 学习数据模型

- localStorage 主键为 `historyMatchUserV2`；旧键只用于首次迁移音效和停留时间设置。
- V2 数据包含：`profileCache`、`sessions`、`events`、`mastery`、`pendingSync`、`settings`、`daily`。
- `profileCache` 优先读取公司未来注入的 `window.LEARNING_DEVICE_CONTEXT`，演示环境使用明确的 demo 身份。
- 每局生成唯一 `sessionId`；每个行为事件生成唯一 `eventId`。
- 必须记录开始、翻牌、错误配对、完成配对、使用提示、完成对局和中途退出。
- `pairResults` 至少包含 `pairId`、`knowledgeId`、有效错误次数、原始错误次数、首次翻开豁免次数、尝试次数、提示使用、线索可见和完成状态。
- 每张卡片在本局第一次由学生翻开时属于未知信息；该次翻牌参与错配仍记录原始行为，但不计入“独立判断正确率”和知识掌握度的有效错误。已经通过提示看过的卡片不再属于首次未知翻牌。
- 完成对局时保存 `independentAccuracy` 和 `assessedWrongAttempts`；教师端与后台统计必须使用有效错误口径，不得直接使用原始错配数扣减正确率。
- 游戏积分与知识掌握度分离；游戏积分仍为 0–1000，独立判断表现占 90%，速度占 10%。积分使用有效错误密度、棋盘负荷容忍系数和完成鼓励基线，3×3、4×4、5×5分别按独立参数校准。评分映射以750分为中心聚集常见表现，并保留900–1000分的自然优秀尾部；不得按排名限制高分人数。
- 速度分使用平滑非线性衰减：刚超过参考时限时下降稍快，时间进一步增加后衰减逐渐放缓；两倍参考时限且无有效错误时仍应得到约950分。
- 知识证据按有效错误、棋盘规模和题目认知层级校准；3×3高分属于初步证据，4×4与5×5稳定表现提供更强证据。教师端必须同时显示证据次数与可信度，避免用单次高分直接判定掌握。
- 线索可见只施加 3% 低权重影响，主动提示只施加 5% 低权重影响，但原始字段必须永久保留。
- 掌握度使用最近五次证据加权，权重依次为 40%、25%、15%、12%、8%。
- 板块掌握度必须同时考虑已测表现和知识覆盖度。未遇到的知识点不计为错误，使用 35 分中性先验参与校准；覆盖率可靠性权重为 `coverage^0.65`，覆盖越充分，实际表现权重越高。学生端成长报告与教师端必须展示“已覆盖/题库总量”。
- 教学证据状态分为：覆盖率低于 30% 的“证据不足”、覆盖充分但掌握度低于 65 的“暴露薄弱”、正在形成稳定证据的“持续积累”，以及掌握度不低于 85 且覆盖率不低于 60% 的“稳定掌握”。教师端统一把“暴露薄弱”人数显示为“需要巩固”，不得使用含义模糊的“待关注”，也不得把尚未测够的学生直接判断为薄弱。
- 掌握状态为：已掌握、基本掌握、需要巩固、重点复习。
- 本地记录默认不自动清理；设置中的主动清除会保留设置并重建V2空数据。

## 后台同步边界

- 当前只建立 `pendingSync` 本地队列和禁用状态的 HTTPS 接口占位，不发送真实请求。
- HTML 中禁止写死令牌、密码或真实学生个人信息。
- 后续真实接入必须由公司SDK或容器提供学生、班级、年级身份与短期鉴权。
- 后台必须依据 `eventId` 幂等去重，并支持断网后批量补传。
- 教师端不得在学生游戏内展示入口；独立页面通过同源 `historyMatchUserV2` 读取本机记录。
- 教师端当前为汇报演示后台：筛选入口只按班级划分，不提供年级筛选；页面内部仍可保留年级字段以兼容公司后续身份与接口规范。本机学生区域使用真实本地记录。
- 教师端常规界面隐藏“仿真实后端模式”、演示环境人数等技术性标识；这些运行信息只在同步状态或排障区域展示，避免干扰教师阅读教学数据。
- 教师端直接以文件方式打开时，只保留3名固定匿名学生和少量固定对局作为界面演示，不再批量随机生成多班级学习记录；HTTP远端模式仍以真实演示API返回的数据为准。
- `SYNC_SPEC.md` 是既有同步方案文档，除非用户明确要求，否则不要修改。

## 演示后端

- `app.py` 是 Flask + SQLAlchemy 演示服务；本地默认使用 SQLite，设置 `DATABASE_URL` 后可使用 PostgreSQL。
- HTTP 环境由同一服务提供 `/student`、`/teacher` 和 `/api/v1`，避免演示阶段跨域；直接打开 HTML 时继续使用离线模式。
- 学生事件通过 `/api/v1/learning-events/batch` 每批最多上传50条，服务端必须按 `eventId` 幂等确认。
- 演示学生只使用 `demo-s...` 匿名编号；禁止在数据库种子或前端中加入真实姓名、学号和联系方式。
- `render.yaml` 仅用于黑客松公网演示，正式公司接入时应替换身份、鉴权、数据库和部署配置。
- 本地运行与重置方式记录在 `README.md`；重置接口必须通过 `DEMO_ADMIN_KEY` 保护。

## 布局与安全实现

- 学练机横屏游戏页使用三列：左侧线索，中间棋盘，右侧导航、指标、关卡介绍和重新开始。
- 棋盘使用浅薄荷绿色底板与青绿色卡背，不使用深蓝、深紫或金橙色底板；5×5 在 1920×1200 及学练机缩放视口下均须完整显示，页面不得异常滚动。
- 卡片内容通过 `textContent` 写入，不拼接不受控HTML。
- 成长报告入口在侧栏内居中；报告顶部必须提供无需滚动即可点击的“知道了”退出按钮。
- 成长报告与成就墙使用米白、青绿、铜金、朱砂主题；弹窗滚动时标题和“知道了”按钮必须固定在不透明顶栏内，不得与下方卡片穿插重叠。
- 修改样式后同时检查三种棋盘、首页、成长报告、教师看板和 720px 以下回退布局。
- 学生端与教师端统一采用明亮暖色视觉：米白为主背景，暖黄与珊瑚橙承担重点行动，青绿色承担结构与学习状态；避免大面积蓝色。1920×1200 下左右栏必须顶边、底边和主要内容基线对齐，教师端不得产生页面级横向溢出。
- 学练机内置 WebView 可能受设备像素比、系统状态栏和导航栏影响，把 1920×1200 物理屏幕报告为约 800×480、960×540 或 960×600 CSS 视口；横屏宽度不低于760且高度不低于480时仍按学练机单屏布局处理，不得误入移动端单栏。真正窄屏或高度低于480的移动端保持自然滚动。
- 缩放后的学练机游戏页仍使用左侧线索、中间棋盘、右侧控制三栏；`.game-head` 必须在该断点使用 `display: contents`，使导航与指标进入主网格，避免其包裹棋盘并把卡片压缩到左上角。
- 学练机还可能报告为宽度不低于1400但可用高度低于900的 CSS 视口；该区间必须继续复用缩放版三栏规则，响应式断点不得在1399/1400之间留下未覆盖空档。
- 学练机横屏游戏区最终采用18%线索、62%棋盘、20%控制区的强制三栏比例；棋盘明确占据第二列、顶部对齐并设置 `min-width:0`，避免内置 WebView 的字体缩放或固有尺寸计算把棋盘压缩成窄缝、完全挤出视口或因自动外边距产生过多下方留白。
- 棋盘尺寸同时受中间列宽、960像素上限和当前可视高度约束；常规横屏保留64像素安全边距，CSS可视高度不高于650像素的学练机WebView只保留20像素边距并压缩游戏区上下内边距。支持 `svh` 时优先使用稳定可视高度，兼顾桌面不溢出与低高度设备尽量放大棋盘。
- 学练机横屏必须锁定为单屏且禁止页面级纵向滚动。除原生1920×1200视口外，还需兼容因Windows显示缩放而报告为约960×600 CSS像素的学练机视口；该视口仍使用横屏三栏游戏布局，不得误入移动端。真正的小屏设备（宽度低于900px或横屏高度低于560px）使用自然文档流并允许纵向滚动。
- 保持单文件运行，不无故引入框架、外部字体、外部脚本或网络依赖。

## 修改后的验证清单

- 验证三个知识板块和三种棋盘均可开始并完成游戏。
- 检查每局只有一种关系，普通卡数量、幸运星和配对分母正确。
- 验证匹配、失败盖回、快速点击锁、重新开始和中途退出事件。
- 验证线索数量、单卡提示、持续高亮、提示编号和不计步规则。
- 验证题库各板块每种关系至少12组，ID稳定、文本无冲突、史实准确。
- 验证完成对局后生成 session、events、pairResults、mastery 和 pendingSync。
- 验证成长报告按三个板块显示掌握度；独立教师后台支持年级、多班级和本机学生画像。
- 验证刷新后V2数据、设置和每日挑战恢复，旧版只迁移设置。
- 保持 `SYNC_SPEC.md` 内容不变，除非用户另行明确授权。
