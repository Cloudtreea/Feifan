# 数学对对碰：项目协作说明

## 项目定位

- 项目是一个浏览器端单机数学记忆翻牌游戏，名称为“数学对对碰”。
- 唯一应用文件是 `memory_station.html`；HTML、CSS、JavaScript 均内嵌其中。
- 保持零依赖、无构建步骤、无服务器，直接用现代浏览器打开文件即可运行。
- 界面与文案统一使用简体中文。

## 产品目标

- 用短回合、低门槛的翻牌玩法改善学生“执行启动感”低的问题。
- 用明确任务、即时匹配反馈和成长记录改善学生“方法掌控感”弱的问题。
- 将数学复习、位置记忆和轻量游戏体验结合起来，不把产品做成传统刷题页面。

## 主要功能

- 提供基础、进阶、挑战三档游戏难度，不按小学、初中、高中划分知识。
- 提供 3×3、4×4、5×5 三种棋盘。
- 支持翻牌动画、匹配判断、失败自动盖回、计时、翻牌计数、得分和结果反馈。
- 奇数格棋盘加入一张幸运星；翻开后直接完成一组。
- 提供日期固定题组的每日挑战、本地打卡、连续天数、历史成绩和分难度柱状图。
- 提供浏览器生成音效、失败停留时间设置和本地记录清除。

## 题库结构与约束

- 题库位于 `BANK`，每组格式为 `[左卡文本, 右卡文本, 类型]`。
- 左右文本不同的题组显示“题目”和“答案”；左右文本相同的题组显示两张“公式”卡。
- 仅使用高考以前的数学知识，不加入大学数学内容。
- 知识点应短小、简单、适合卡片阅读；不要为了扩充数量而加入低价值内容。
- 不加入“一周 = 七天”“直角 = 90°”等生活常识或过于基础的常识卡。
- 数值通常不超过 100，优先使用小数以及 2、5 的倍数，避免大量大几十数值。
- 算式答案必须写成完整等式，例如 `12 − 5 = 7`，不能只写结果 `7`。
- 同局内不能出现答案相互冲突或可交叉配对的题组，必须保证唯一合理配对。
- 控制同类题数量，分散运算、方程、图形、函数、统计等内容。
- 基础难度允许用两张完全相同的简单公式作为一组。
- 向量必须使用标准头顶箭头。题库文本写成 `\\vec{a} · \\vec{b} = 0`，由 `setMathText()` 转换为 `.vector-letter` 元素并用 CSS 绘制箭头；不要使用容易显示成方框的组合 Unicode 箭头。
- 修改题库后，确保每档至少能抽取 12 组有效题目，以支持 5×5 棋盘。

## 抽题与游戏状态

- `selectPairs()` 负责打乱题库、均衡类型并排除文本重复；不要绕过该函数直接抽题。
- 文本去空格并转小写后参与冲突检测。新增题目时仍需人工检查语义冲突，不能只依赖字符串比较。
- 每局普通卡片按共同 `id` 判断匹配，而不是直接比较显示文本。
- 任一时刻最多只能有两张尚未完成匹配的普通卡片正面朝上。
- 匹配成功的两张卡片保持翻开并显示绿色；未匹配卡片按设置延迟自动盖回。
- 重新开始时必须重置计时器、翻牌数、配对数、首张卡引用和交互锁。
- 每日挑战使用日期、难度和棋盘规模生成固定随机种子；同一天同配置应得到相同题组。

## 卡片与响应式布局

- 棋盘必须使用与页面不同的深紫色背景，确保卡片边界清晰。
- `render()` 将棋盘规模写入 `board.dataset.size`；CSS 据此为 3×3、4×4、5×5 使用不同字号。
- 卡片文本通过 `textContent` 写入 `.card-content`，不要改成直接拼接不受控 HTML。
- 文本长度超过 9 个 Unicode 字符时使用 `long`，超过 13 个时使用 `very-long`。
- 保持 `word-break: keep-all` 和 `text-wrap: balance`，优先在空格或语义边界换行。
- 修改卡片样式后，同时检查三种棋盘和 720px 以下窄屏，避免文字溢出、标签遮挡和公式拆分。

## 计分与本地数据

- 最终得分范围是 0–1000，精准度占 70%，速度占 30%。
- 速度目标为：3×3 35 秒、4×4 75 秒、5×5 130 秒。
- localStorage 键名固定为 `memoryStationUserV2`，不要无迁移方案地更名。
- 数据结构包含：`history`、`settings`、`daily`。
- `history` 单条记录包含日期、难度、棋盘规模、翻牌数、秒数、得分和每日挑战标记，最多保留 50 条。
- `settings` 保存音效开关和失败卡停留时间，默认值为 `sound: true`、`delay: 900`。
- `daily` 保存已完成每日挑战的 ISO 日期字符串。
- 清除记录时保留设置，仅清空历史成绩和打卡记录。
- 读取数据时容忍 localStorage 缺失或 JSON 损坏，不应阻断游戏启动。

## 修改后的验证清单

- 验证首页难度和棋盘选择、开始游戏、返回主页与重新开始。
- 验证匹配成功、匹配失败、交互锁、幸运星和整局完成判定。
- 验证三种棋盘实际生成的卡片数量和配对进度分母正确。
- 多次抽题，确认同局题型不过度重复、没有交叉答案和已删除题目。
- 验证长公式、中文知识点和带箭头向量在三种棋盘中均清晰可读。
- 验证刷新页面后设置、历史、今日挑战状态和连续打卡能够恢复。
- 验证成长报告分别统计基础、进阶、挑战三档数据，空数据时正常显示。
- 保持单文件离线运行，不无故引入框架、外部资源或网络请求。
